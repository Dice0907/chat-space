.chat
  .header
    .left-header
      .left-header__title
        = @group.name
        %ul.left-header__members
        Member：
        %li.member
        - @users.each do |user|
          = user.name
        
    .right-header
      .right-header__button         
        = link_to "Edit", edit_group_path(params[:group_id]), method: :get